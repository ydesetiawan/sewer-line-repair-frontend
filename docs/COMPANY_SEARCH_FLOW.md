# Company Search - Visual Flow Diagram

## 🔍 Search Flow Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  🔍  Search by company name...                    [✖] │    │
│  └───────────────────────────────────────────────────────┘    │
│                           │                                     │
│                           │ @input event                       │
│                           ▼                                     │
│  ┌───────────────────────────────────────────────────────┐    │
│  │              handleSearch()                           │    │
│  │  • Set isSearching = true                            │    │
│  │  • Clear previous timeout                            │    │
│  │  • Wait 500ms (debounce)                            │    │
│  └───────────────────────────────────────────────────────┘    │
│                           │                                     │
│                           │ After 500ms                        │
│                           ▼                                     │
│  ┌───────────────────────────────────────────────────────┐    │
│  │              fetchData(1, false)                      │    │
│  │  • Reset to page 1                                    │    │
│  │  • Build API request with searchQuery                │    │
│  │  • Show loading spinner                               │    │
│  └───────────────────────────────────────────────────────┘    │
│                           │                                     │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API LAYER                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  GET /api/v1/states/{state}/companies                          │
│  ?company_name=pro%20plumb                                     │
│  &page=1                                                        │
│  &per_page=20                                                   │
│                                                                 │
│                           │                                     │
│                           │ API Response                        │
│                           ▼                                     │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  {                                                     │    │
│  │    data: [/* filtered companies */],                 │    │
│  │    meta: {                                             │    │
│  │      pagination: { total_items: 5, ... }             │    │
│  │    }                                                   │    │
│  │  }                                                     │    │
│  └───────────────────────────────────────────────────────┘    │
│                           │                                     │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                      STATE UPDATE                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  • companies.value = response.data                             │
│  • pagination.value = response.meta.pagination                 │
│  • isSearching.value = false                                   │
│                                                                 │
│                           │                                     │
│                           │ Reactive update                     │
│                           ▼                                     │
└───────────────────────────┼─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                        UI UPDATE                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  Search Results                                        │    │
│  │  Found 5 results for "pro plumb"                      │    │
│  │  Showing 5 of 5 companies                             │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
│  ┌───────────────────────────────────────────────────────┐    │
│  │  [Company Card 1]                                      │    │
│  │  [Company Card 2]                                      │    │
│  │  [Company Card 3]                                      │    │
│  │  [Company Card 4]                                      │    │
│  │  [Company Card 5]                                      │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 State Transitions

```
┌──────────────┐
│   IDLE       │  No search query
│   ────       │  Shows all companies
└──────┬───────┘
       │
       │ User types
       ▼
┌──────────────┐
│  DEBOUNCING  │  isSearching = true
│  ⌛ ────────  │  Spinner shows
└──────┬───────┘  Waiting 500ms
       │
       │ Timeout completes
       ▼
┌──────────────┐
│   LOADING    │  API call in progress
│  ⌛ ────────  │  Fetching filtered data
└──────┬───────┘
       │
       │ Response received
       ▼
┌──────────────┐
│   RESULTS    │  isSearching = false
│   ✓ ────     │  Companies displayed
└──────┬───────┘
       │
       │ Click X button
       ▼
┌──────────────┐
│   CLEARING   │  searchQuery = ''
│  ⌛ ────────  │  Fetching all companies
└──────┬───────┘
       │
       │ Response received
       ▼
┌──────────────┐
│   IDLE       │  Back to showing all
│   ────       │  
└──────────────┘
```

---

## 🎭 User Interaction Flows

### Flow 1: Basic Search

```
User Action                 System Response
───────────                 ───────────────

Types "pro"
                           → Update searchQuery
                           → Start debounce timer
                           → Show spinner ⌛

Continues typing "plumb"
                           → Clear previous timer
                           → Start new 500ms timer
                           → Keep showing spinner ⌛

Stops typing
                           → Wait for timer...
                           → Timer completes (500ms)
                           → Call API with "pro plumb"
                           → Fetch filtered results

API responds
                           → Update companies list
                           → Hide spinner
                           → Show "Found X results"
                           → Display company cards
```

### Flow 2: Clear Search

```
User Action                 System Response
───────────                 ───────────────

Has active search
"pro plumb"                → Showing 5 results
                           → X button visible

Clicks X button
                           → Clear searchQuery
                           → Reset to page 1
                           → Call API (no filter)
                           → Show loading

API responds
                           → Update companies list
                           → Show "All Contractors"
                           → Display all companies
                           → Hide X button
```

### Flow 3: No Results

```
User Action                 System Response
───────────                 ───────────────

Types "xyz company"
                           → Debounce 500ms
                           → Call API

API responds (empty)
                           → companies.length = 0
                           → Show empty state:
                             🔍
                             "No Results Found"
                             "Clear Search" button
```

---

## 📊 Component State Diagram

```
┌─────────────────────────────────────────┐
│         CompanyList Component           │
├─────────────────────────────────────────┤
│                                         │
│  State Variables:                       │
│  ┌────────────────────────────────┐   │
│  │ searchQuery: ref('')           │   │
│  │ isSearching: ref(false)        │   │
│  │ companies: ref([])             │   │
│  │ pagination: ref(null)          │   │
│  │ loading: ref(false)            │   │
│  │ currentPage: ref(1)            │   │
│  └────────────────────────────────┘   │
│                                         │
│  Computed:                              │
│  ┌────────────────────────────────┐   │
│  │ totalCompanies                 │   │
│  │ loadedCount                    │   │
│  │ hasMorePages                   │   │
│  └────────────────────────────────┘   │
│                                         │
│  Methods:                               │
│  ┌────────────────────────────────┐   │
│  │ handleSearch()                 │   │
│  │ clearSearch()                  │   │
│  │ fetchData()                    │   │
│  │ loadMore()                     │   │
│  └────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

---

## ⚡ Performance Timeline

```
User Action                Time        System State
───────────                ────        ────────────

Keystroke 1: "p"          0ms         searchQuery = "p"
                                      Debounce starts
                                      isSearching = true

Keystroke 2: "r"          50ms        searchQuery = "pr"
                                      Previous timer cleared
                                      New timer starts

Keystroke 3: "o"          100ms       searchQuery = "pro"
                                      Previous timer cleared
                                      New timer starts

User stops typing         150ms       Timer counting...

Timer completes           650ms       Call API
                                      Still showing spinner

API call                  650-1150ms  Network request

API response              1150ms      Update UI
                                      isSearching = false
                                      Show results

Total time: ~1150ms from last keystroke to results
```

---

## 🔐 Data Flow Security

```
┌──────────────┐
│  User Input  │
└──────┬───────┘
       │
       │ Sanitization (automatic)
       ▼
┌──────────────┐
│  searchQuery │  Vue's reactive system
└──────┬───────┘  Ensures safe binding
       │
       │ URL encoding
       ▼
┌──────────────┐
│  API Request │  company_name=encoded%20value
└──────┬───────┘
       │
       │ Server validation
       ▼
┌──────────────┐
│  Filtered    │  Safe, validated results
│  Results     │
└──────────────┘
```

---

## 🎨 UI Component Tree

```
CompanyList
├── Loading State (v-if="loading")
│   └── Loader2 Icon
│
├── Error State (v-else-if="error")
│   ├── AlertCircle Icon
│   └── Try Again Button
│
└── Content (v-else)
    ├── Header
    │   ├── H1: Title
    │   └── P: Description
    │
    ├── Search Bar ← NEW!
    │   ├── Label
    │   └── Input Container
    │       ├── Search Icon (left)
    │       ├── Input Field (v-model)
    │       └── Right Icons
    │           ├── Loader2 (v-if="isSearching")
    │           └── X Button (v-if="searchQuery")
    │
    ├── Cities Grid (v-if="cities.length > 0")
    │   └── CityCard (v-for)
    │
    └── Company Cards
        ├── Header
        │   ├── Dynamic Title ← NEW!
        │   └── Result Count ← NEW!
        │
        ├── No Results State (v-if) ← NEW!
        │   ├── Search Icon
        │   ├── Message
        │   └── Clear Button
        │
        ├── Company Cards (v-else)
        │   └── CompanyCard (v-for)
        │
        └── Load More Button
```

---

## 🔧 Configuration

### Debounce Settings
```typescript
const DEBOUNCE_DELAY = 500 // milliseconds
```

### API Parameters
```typescript
{
  page: number,           // Page number
  per_page: 20,          // Items per page
  city: string | null,   // City filter
  company_name: string | null  // Search query ← NEW!
}
```

### Loading States
```typescript
loading: boolean        // Initial load
loadingMore: boolean   // Load more button
isSearching: boolean   // Search in progress ← NEW!
```

---

This visual flow diagram shows exactly how the search functionality works from user input to displaying results!

